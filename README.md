# Desktop Organizer Pro

Windows와 macOS에서 사용 가능한 데스크톱 파일 정리 프로그램입니다.

## ✨ 주요 기능

### 📁 파일 정리
- **바탕화면 정리**: 파일을 유형별로 자동 분류 (이미지, 문서, 비디오, 음악, 압축파일 등)
- **드래그 앤 드롭**: 파일을 카테고리 카드로 드래그하여 즉시 정리
- **규칙 기반 정리**: 사용자 정의 규칙으로 자동 정리
- **일괄 이름 변경**: 접두사/접미사, 일련번호, 날짜, 정규식 기반 파일명 변경
- **확장자 분류**: 확장자별 또는 카테고리별로 파일 정리
- **폴더 매니저**: 폴더 트리 시각화, 중복/빈폴더/대용량 파일 검색

### ⌨️ 사용성 향상
- **키보드 단축키**: 10+ 단축키로 빠른 작업 (Ctrl+A, Ctrl+F, Space 등)
- **우클릭 메뉴**: 파일에 빠른 액션 (미리보기, 이름 변경, 이동, 복사, 삭제 등)
- **다중 선택**: Shift+Click으로 범위 선택, 체크박스 선택
- **QuickLook 미리보기**: Space 키로 파일 미리보기 (이미지 지원)
- **고급 검색**: 카테고리, 크기, 날짜, 확장자로 필터링

### 🎨 UI/UX
- **빈 상태 메시지**: 상황별 안내 메시지
- **스켈레톤 로더**: 부드러운 로딩 경험
- **에러 처리**: 사용자 친화적 에러 메시지 + 재시도
- **툴팁**: 도움말 + 키보드 단축키 힌트
- **애니메이션**: Framer Motion으로 세련된 인터랙션

## 기술 스택

- **Frontend**: React 18, TypeScript, Tailwind CSS, shadcn/ui, Framer Motion
- **Backend**: Tauri v2 (Rust)
- **Database**: SQLite

## 개발 환경 설정

### 사전 요구사항

- Node.js 18+
- Rust (https://rustup.rs)
- 플랫폼별 빌드 도구:
  - **Windows**: Microsoft C++ Build Tools
  - **macOS**: Xcode Command Line Tools

### 설치 및 실행

```bash
# 의존성 설치
npm install

# 개발 모드 실행
npm run tauri dev

# 프로덕션 빌드
npm run tauri build
```

## 📊 프로젝트 통계

- **TypeScript 파일**: 95개
- **컴포넌트**: 50+ 개
- **커스텀 훅**: 5개
- **번들 크기**: 661 KB (197 KB gzipped)
- **지원 단축키**: 10개

## 📦 프로젝트 구조

```
├── src/                      # React 프론트엔드
│   ├── components/           # UI 컴포넌트
│   │   ├── ui/               # shadcn/ui primitives (40+)
│   │   ├── RuleManagement/   # 규칙 관리
│   │   ├── DesktopView.tsx   # 메인 뷰
│   │   ├── FileContextMenu.tsx
│   │   ├── KeyboardShortcutsHelp.tsx
│   │   ├── SelectionToolbar.tsx
│   │   ├── AdvancedSearch.tsx
│   │   ├── QuickLookModal.tsx
│   │   └── ...
│   ├── hooks/                # 커스텀 훅
│   │   ├── useKeyboardShortcuts.ts
│   │   ├── useFileDragDrop.ts
│   │   ├── useFileSelection.ts
│   │   └── useFileNavigation.ts
│   ├── constants/            # 공유 상수
│   │   ├── categories.ts
│   │   ├── icons.ts
│   │   └── errorMessages.ts
│   ├── utils/                # 유틸리티
│   │   └── errorHandler.ts
│   ├── stores/               # Zustand 상태 관리
│   │   ├── fileStore.ts
│   │   └── uiStore.ts
│   └── lib/                  # 라이브러리
│       ├── tauri-api.ts
│       ├── types.ts
│       └── utils.ts
├── src-tauri/                # Rust 백엔드
│   ├── src/
│   │   ├── commands/         # Tauri IPC 명령
│   │   ├── database/         # SQLite 작업
│   │   └── services/         # 비즈니스 로직
│   └── Cargo.toml
└── package.json
```

## ⌨️ 키보드 단축키

| 단축키 | 기능 |
|--------|------|
| `Ctrl+A` | 모두 선택 / 선택 해제 |
| `Ctrl+F` | 검색 포커스 |
| `Ctrl+O` | 자동 정리 |
| `Ctrl+Z` | 되돌리기 |
| `Ctrl+R` | 새로고침 |
| `Space` | 빠른 보기 (QuickLook) |
| `Delete` | 선택한 파일 삭제 |
| `Escape` | 선택 해제 / 패널 닫기 |
| `Ctrl+/` or `?` | 단축키 도움말 |

## 🛠️ 개발 스크립트

```bash
npm run dev              # Vite 개발 서버
npm run tauri:dev        # Tauri 개발 모드
npm run build            # 프로덕션 빌드
npm run tauri:build      # Tauri 앱 빌드
npm run lint             # ESLint 검사
npm run lint:fix         # ESLint 자동 수정
npm run type-check       # TypeScript 타입 체크
npm run format           # Prettier 포맷팅
npm run test             # 테스트 실행
npm run clean            # 빌드 캐시 정리
```

## 라이선스

MIT
